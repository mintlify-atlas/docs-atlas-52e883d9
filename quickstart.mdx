---
title: Quick Start
description: Get the EPR LAPS Backend API running locally in minutes
---

## Get Started in 5 Minutes

This guide will help you quickly set up and run the EPR LAPS Backend API on your local machine.

<Steps>
  <Step title="Prerequisites">
    Ensure you have the required tools installed:
    
    - **Node.js** >= v22.16.0
    - **npm** >= v11
    - **MongoDB** (or use Docker Compose)
    
    <Note>
      We recommend using [nvm](https://github.com/creationix/nvm) to manage Node.js versions.
    </Note>

    If using nvm, switch to the correct Node.js version:
    
    ```bash
    cd epr-laps-backend
    nvm use
    ```
  </Step>

  <Step title="Clone and Install">
    Clone the repository and install dependencies:
    
    ```bash
    git clone <repository-url>
    cd epr-laps-backend
    npm install
    ```
    
    The installation will automatically set up Husky git hooks for code quality checks.
  </Step>

  <Step title="Configure Environment">
    Set up your environment variables. Create a `.env` file or export the following:
    
    <CodeGroup>
    ```bash Development
    export NODE_ENV=development
    export PORT=3001
    export HOST=0.0.0.0
    export MONGO_URI=mongodb://127.0.0.1:27017/
    export MONGO_DATABASE=epr-laps-backend
    export DEFRA_ID_DISCOVERY_URL=http://localhost:3200/cdp-defra-id-stub/.well-known/openid-configuration
    export DEFRA_ID_ISSUER=http://localhost:3200/cdp-defra-id-stub
    export FSS_API_URL=http://localhost:3003/api
    export FSS_API_KEY=some-api-key
    ```

    ```bash Production
    export NODE_ENV=production
    export PORT=3001
    export MONGO_URI=mongodb://your-mongo-host:27017/
    export MONGO_DATABASE=epr-laps-backend
    export DEFRA_ID_DISCOVERY_URL=https://your-auth-server/.well-known/openid-configuration
    export DEFRA_ID_ISSUER=https://your-auth-server
    export FSS_API_URL=https://your-fss-api/api
    export FSS_API_KEY=your-secure-api-key
    export FSS_API_ENCRYPTION_KEY=your-base64-encryption-key
    ```
    </CodeGroup>

    <Warning>
      Never commit sensitive values like `FSS_API_KEY` or `FSS_API_ENCRYPTION_KEY` to version control.
    </Warning>
  </Step>

  <Step title="Start the Server">
    Run the API in development mode:
    
    ```bash
    npm run dev
    ```
    
    The server will start on `http://0.0.0.0:3001` with hot-reloading enabled.
    
    <Note>
      Development mode includes:
      - Automatic restart on file changes (via nodemon)
      - Debug inspector on port 9229
      - Pretty-printed logs
    </Note>
  </Step>

  <Step title="Verify Installation">
    Test the API is running by checking the health endpoint:
    
    ```bash
    curl http://localhost:3001/health
    ```
    
    Expected response:
    ```json
    {
      "status": "ok"
    }
    ```
  </Step>
</Steps>

## Alternative: Docker Compose

For a complete local development environment with all dependencies, use Docker Compose:

<Steps>
  <Step title="Start Docker Environment">
    Launch the entire stack:
    
    ```bash
    docker compose up --build -d
    ```
    
    This starts:
    - EPR LAPS Backend API on port 3001
    - MongoDB on port 27017
    - Redis on port 6379
    - Localstack (AWS services) on port 4566
  </Step>

  <Step title="Verify Services">
    Check all services are running:
    
    ```bash
    docker compose ps
    ```
    
    Test the API:
    ```bash
    curl http://localhost:3001/health
    ```
  </Step>

  <Step title="View Logs">
    Monitor application logs:
    
    ```bash
    docker compose logs -f epr-laps-backend
    ```
  </Step>

  <Step title="Stop Environment">
    When finished:
    
    ```bash
    docker compose down
    ```
  </Step>
</Steps>

## Available Scripts

The following npm scripts are available for development:

<CodeGroup>
```bash Development
# Start with hot-reload and debugging
npm run dev

# Start with breakpoint debugging
npm run dev:debug
```

```bash Testing
# Run tests with coverage
npm test

# Run tests in watch mode
npm run test:watch
```

```bash Linting & Formatting
# Check code formatting
npm run format:check

# Auto-fix formatting issues
npm run format

# Lint JavaScript files
npm run lint

# Auto-fix linting issues
npm run lint:fix
```

```bash Production
# Run in production mode
npm start
```
</CodeGroup>

## Test the API Endpoints

Once running, you can test the API endpoints:

### Health Check

```bash
curl http://localhost:3001/health
```

### Bank Details (requires authentication)

```bash
curl http://localhost:3001/bank-details/LA123 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Documents (requires authentication)

```bash
curl http://localhost:3001/documents/LA123 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

<Note>
  For authenticated endpoints, you'll need to obtain a valid JWT token from your Defra ID provider.
</Note>

## Development Features

### Hot Reloading

The development server uses nodemon to automatically restart when you modify:
- JavaScript files (`.js`)
- JSON files (`.json`)

### Debug Mode

Connect a debugger to port 9229:

```bash
# VS Code launch.json
{
  "type": "node",
  "request": "attach",
  "name": "Attach to Process",
  "port": 9229,
  "restart": true
}
```

### MongoDB Locks

For write operations requiring locks:

```javascript
import { createServer } from './server.js'

const server = await createServer()

async function doStuff(server) {
  const lock = await server.locker.lock('unique-resource-name')

  if (!lock) {
    // Lock unavailable
    return
  }

  try {
    // Perform write operations
  } finally {
    await lock.free()
  }
}
```

### Proxy Configuration

The API includes forward-proxy support using undici:

```javascript
import { fetch } from 'undici'

// Automatically uses proxy if HTTP_PROXY env var is set
const response = await fetch('https://api.example.com/data')
```

## Troubleshooting

<AccordionGroup>
  <Accordion title="Connection refused to MongoDB">
    Ensure MongoDB is running:
    ```bash
    # Check if MongoDB is running
    mongosh --eval "db.adminCommand('ping')"
    
    # Or use Docker Compose
    docker compose up -d mongodb
    ```
  </Accordion>

  <Accordion title="Port 3001 already in use">
    Change the port via environment variable:
    ```bash
    export PORT=3002
    npm run dev
    ```
  </Accordion>

  <Accordion title="Prettier formatting issues on Windows">
    Update your global git config:
    ```bash
    git config --global core.autocrlf false
    ```
  </Accordion>

  <Accordion title="Module not found errors">
    Reinstall dependencies:
    ```bash
    rm -rf node_modules package-lock.json
    npm install
    ```
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Installation Guide"
    icon="book"
    href="/installation"
  >
    Detailed installation and configuration instructions
  </Card>
  <Card
    title="API Reference"
    icon="code"
    href="/api-reference"
  >
    Complete API endpoint documentation
  </Card>
</CardGroup>